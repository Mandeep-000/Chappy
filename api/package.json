{
  "scripts": {
    "install-dependencies": "npm run install-client && npm install",
    "setup-production": "npm install && npm run install-client && npm run build-client",
    "install-client": "cd ../client && npm install",
    "build-client": "cd ../client && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.2.1",
    "ws": "^8.16.0"
  }
}